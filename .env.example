# Copy to .env on the Droplet and fill in real values. Do NOT commit .env.
#   cp .env.example .env && nano .env
POSTGRES_DB=featureflags
POSTGRES_USER=featureflags
# Use a strong random value, e.g. `openssl rand -base64 24`
POSTGRES_PASSWORD=change-me-to-a-strong-secret
